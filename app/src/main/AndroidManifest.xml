<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.panduanpenggunaanubat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pills_background"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".penInsulinAllstar"></activity>
        <activity android:name=".penInsulinNovopen" />
        <activity android:name=".ubatPamEasyhaler" />
        <activity android:name=".ubatPamAccuhaler" />
        <activity android:name=".ubatPamTurbuhaler" />
        <activity android:name=".ubatPamMDI" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>